<xml type="Scenario" name="Extract Variable" id="php.CodeGolf.ExtractVariable" lang="PHP">
    <MouseBlock>
        <CopyText><![CDATA[<?php

function report(string $inputName)
{
    return (empty($inputName) ? "Unknown" : $inputName) . " is here.";
}]]></CopyText>
        <CaretBlock>
            <SetSelection start-position="5:12" end-position="5:56" />
            <MoveCaret position="5:36" />
            <Try triggers="IntroduceVariable;NextTemplateVariable">Extracting nontrivial expression to a variable may make code way easier to read, and sometimes faster to run. Press <action>IntroduceVariable</action> to do it.</Try>
            <Win/>
        </CaretBlock>
    </MouseBlock>
</xml>